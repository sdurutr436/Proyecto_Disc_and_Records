# ===========================================
# Variables de entorno para DESARROLLO LOCAL
# Copia este archivo como .env en la carpeta backend/
# ===========================================

# --- JWT para Desarrollo Local ---
# IMPORTANTE: NO uses el mismo secreto que en producción
# Este es un secreto de ejemplo solo para desarrollo
JWT_SECRET=local-development-secret-only-do-not-use-in-production-12345
JWT_EXPIRATION=86400000

# --- CORS (opcional, ya está configurado por defecto) ---
# Si necesitas permitir más orígenes en desarrollo:
# CORS_ALLOWED_ORIGINS=http://localhost:4200,http://localhost:3000,http://192.168.1.100:4200

# ===========================================
# INSTRUCCIONES:
# 
# 1. Copia este archivo como .env:
#    cp .env.local.example .env
#
# 2. O configura las variables en tu terminal:
#    PowerShell: $env:JWT_SECRET="tu-secreto-local"
#    Bash/Zsh: export JWT_SECRET="tu-secreto-local"
#
# 3. Ejecuta el backend:
#    ./mvnw spring-boot:run
#
# NOTA: El archivo .env está en .gitignore y NO se subirá a GitHub
# ===========================================
