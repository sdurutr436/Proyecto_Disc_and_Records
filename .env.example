# ===========================================
# Configuración de Docker Compose - LOCAL
# Copiar a .env para usar valores personalizados
# ===========================================

# --- Base de datos MariaDB ---
MYSQL_DATABASE=discsandrecords
MYSQL_USER=discuser
MYSQL_PASSWORD=discpass
MYSQL_ROOT_PASSWORD=rootpass

# --- Backend Spring Boot ---
# JWT Secret - Cambiar a un valor seguro en producción
# Generar: openssl rand -base64 32
JWT_SECRET=mi-clave-secreta-super-segura-de-256-bits-para-jwt-desarrollo-local

# JWT Expiration en milisegundos (86400000 = 24 horas)
JWT_EXPIRATION=86400000

# CORS - Orígenes permitidos (separados por comas)
# En desarrollo:
ALLOWED_ORIGINS=http://localhost:80,http://localhost:4200,http://localhost:8080

# En producción:
# ALLOWED_ORIGINS=https://discsandrecords.com,https://api.discsandrecords.com

# --- Logging ---
# Nivel de log: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO
