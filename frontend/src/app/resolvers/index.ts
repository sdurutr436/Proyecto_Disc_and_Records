/**
 * Barrel export para todos los resolvers de la aplicación
 *
 * Uso:
 * import { albumResolver, artistResolver, songResolver } from '@/resolvers';
 */

export { albumResolver } from './album.resolver';
export { artistResolver } from './artist.resolver';
export { songResolver } from './song.resolver';
